package com.feedbackretriever.app.dao;

import org.springframework.stereotype.Repository;

@Repository("feedbackretrieverrepoimpl")
public abstract class FeedbackRetrieverRepoImpl implements FeedbackRetrieverRepo {

	// Custom methods that are not listed in FeedbackRetrieverRepo(CRUD+MongoDB Repo) to be writen here
}
